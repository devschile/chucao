# Backlog

## Shipped

### 1.0.0 — release-readiness backlog

The 1.0.0 release-readiness backlog (badge status variant, `ch-`/`chucao-` naming
fix, Stencil-starter boilerplate removal, version bump, npm metadata review, and
build/test verification) has been completed and shipped in `1.0.0`.

### CH-02 — Token schema expansion + CSS de-tokenization

- **CH-02.1** Expanded `chucao-tokens.json` from 20 → 61 tokens with new
  categories: `typography.size/weight/tracking`, `spacing` scale (2xs–6xl plus
  `shift`/`elevate`), `radius` (`sm`/`md`/`pill`), `border.width/accent-bar`,
  `shadow` (`focus-ring`/`card-hover`), `duration`, `easing`, `focus`,
  `opacity.disabled`, `z-index`, and `effect.blur`.
- **CH-02.2** Ran `toki build -f stencil` to regenerate `src/tokens/*`
  (`tokens.css`, `tokens.ts`, `tokens.d.ts`, `types.ts`, `README.md`) and
  formatted with Prettier.
- **CH-02.3** Refactored every component stylesheet (`ch-badge`, `ch-button`,
  `ch-card`, `ch-input`, `ch-select`) plus `src/global/chucao.css` to consume
  the new tokens — no hardcoded colors/sizes remain. The `--radius` alias in
  `chucao.css` now maps to the new `--radius-sm` token (was `--spacing-radius`).
- **CH-02.4** Verified: `pnpm run build`, `pnpm test` (21/21), `pnpm lint`
  all green; changed files are Prettier-clean.

### CH-06 — Auto-generated gallery

- **CH-06.1** Added `scripts/generate-gallery.mjs` + `scripts/gallery-data.mjs`:
  the generator scans `src/components/*`, validates every component has a demo
  entry (fails on mismatch), and rewrites the component section of
  `docs-site/index.html` (between `GALLERY:START`/`GALLERY:END` markers) plus the
  `TAG`/event-log wiring in `docs-site/assets/js/app.js`, formatting both with
  Prettier. Run via `pnpm run generate:gallery`; `scripts/` added to the oxlint
  ignore patterns (utility/data files, strict style rules not applicable).
- **CH-06.2** Wired `pnpm run generate:gallery` into `.github/workflows/docs.yml`
  before the publish dir is assembled, so the gallery stays in sync.
- **CH-06.3** Verified: generator is idempotent, output is Prettier-clean, and
  the rendered gallery matches the previous hand-authored sections.

### CH-07 — Docs, changelog, release

- **CH-07.1** Documented the expanded token scale in `DESIGN.md` and the gallery
  generator in `docs/components.md`; added a "Design tokens" section to
  `docs/using-the-library.md`; linked `docs/backlog.md` from the root `README.md`
  docs table.
- **CH-07.2** `pnpm exec release-it --ci` → **1.3.0**: conventional-changelog
  entry, `chore(release): 1.3.0` commit, signed tag `1.3.0`, pushed to `origin/main`
  — triggering the CI release pipeline (build/lint/test, npm publish, CDN deploy).

### CH-03.1 — Validation states on form controls

- **CH-03.1** Added `hint`, `errorMessage`, and `invalid` props to `ch-input`
  and `ch-select`. `hint` renders helper text and is wired via
  `aria-describedby`; when `invalid` + `errorMessage` are set, the hint is
  replaced by the error message (`role="alert"`) and the control gets
  `aria-invalid="true"` plus red border/focus-ring styling via the
  `--color-status-warning*` tokens. Error/hint styles use only design tokens
  (`--typography-size-sm`, `--spacing-sm`, `--color-status-warning*`).
- **CH-03.1** Specs: 8 new tests (4 per component) covering hint/error
  rendering, `aria-describedby`/`aria-invalid` wiring, error-vs-hint
  precedence, and the valid-state omission of error markup. Verified:
  `pnpm lint` (no warnings), `pnpm test` (29/29), `pnpm run build` all green;
  `src/components.d.ts` and both `readme.md` regenerated by the build.

### CH-03.2/03.3 — New form controls

- **CH-03.2** New components, each with `.tsx` + `.css` + `.plugin.spec.tsx` +
  auto-generated `readme.md`:
  - `ch-checkbox` — native `<input type="checkbox">` with implicit-label
    association, `checked` (mutable), `chChange` event, plus the CH-03.1
    `hint`/`errorMessage`/`invalid` validation props.
  - `ch-radio` — radio group driven by a `ChRadioOption[]` `options` prop and
    a selected `value` (mirrors `ch-select`); renders `role="radiogroup"`
    labelled via `aria-labelledby`, auto-generates a shared `name` when none
    is given, emits `chChange` with the selected value.
  - `ch-switch` — native `<input type="checkbox" role="switch">` with a
    track/thumb, `checked` (mutable) reflected in `aria-checked`, `chChange`
    event, and the same validation props.
  - `ch-textarea` — native `<textarea>` with `label`/`rows`/`placeholder`,
    `chInput`/`chChange` events, and the same validation props.
  - All styles are token-driven (`--spacing*`, `--radius*`, `--border-width`,
    `--accent`, `--color-status-warning*`, focus ring/outline tokens) — no
    hardcoded colors/sizes. Invalid state reuses the red
    `--color-status-warning*` treatment from CH-03.1.
- **CH-03.2** Gallery: added demo entries for the four components in
  `scripts/gallery-data.mjs` (alphabetical order) and regenerated
  `docs-site/index.html` + `docs-site/assets/js/app.js` via
  `pnpm run generate:gallery` (9 component blocks). Added `docs-site/assets`
  to the `.oxlintrc.json` ignore patterns (generated JS, same precedent as
  `scripts/`).
- **CH-03.3** Verified: 27 new tests (56 total), full suite green; coverage
  **100%** statements/functions/lines (96.82% branches); `pnpm lint` (no
  warnings) and `pnpm run build` green; new files Prettier-clean. Documented
  the new label/a11y patterns in `docs/components.md`.

### CH-05 — Navigation & content

- **CH-05.1** New components, each with `.tsx` + `.css` + `.plugin.spec.tsx` +
  auto-generated `readme.md`:
  - `ch-link` — native `<a>` with `default`/`muted` variants; auto-adds
    `rel="noopener noreferrer"` for `target="_blank"`; when `disabled` renders
    a non-interactive element with `aria-disabled="true"`. Emits `chClick`.
  - `ch-divider` — native `<hr>` with `horizontal` (default) / `vertical`
    orientations and `aria-orientation`.
  - `ch-tabs` — WAI-ARIA tablist driven by a `ChTab[]` `tabs` prop and a
    selected `value`; arrow-key navigation (Left/Right wrap, Home/End to
    first/last, disabled tabs skipped) with a roving `tabindex`, `aria-selected`
    wiring, and `role="tabpanel"` panels whose consumer content is distributed
    via named `panel-<value>` slots (only the active one visible). Emits
    `chChange` with the selected value.
  - `ch-accordion` — WAI-ARIA accordion with single-open semantics driven by
    `ChAccordionItem[]` `items` and an open `value`; a native `<button>` per
    item with `aria-expanded`/`aria-controls` and `role="region"` panels via
    named `panel-<value>` slots. Emits `chChange` with the new open value
    (`undefined` when all items are closed).
  - All styles are token-driven (`--spacing*`, `--radius*`, `--border-width`,
    `--border-accent-bar`, `--accent*`, `--text*`, focus ring/outline tokens) —
    no hardcoded colors/sizes.
- **CH-05.2** Gallery: added demo entries for the four components in
  `scripts/gallery-data.mjs` (alphabetical order) and regenerated
  `docs-site/index.html` + `docs-site/assets/js/app.js` via
  `pnpm run generate:gallery` (13 component blocks). Documented the new
  `ch-tabs` arrow-key navigation and slotted-panel conventions in
  `docs/components.md`.
- **CH-05.2** Verified: 35 new tests (91 total), full suite green; coverage
  **100%** statements/functions/lines (94.88% branches); `pnpm lint` (no
  warnings) and `pnpm run build` green; new files Prettier-clean.

## Planned — next increment (beyond 1.3.0)

Target: minor release, purely additive (no breaking API/behavior changes).
Form-associated custom elements and multi-theme are intentionally deferred
(form-association needs a 2.0 major; multi-theme is blocked on `toki`).

### CH-01 — PR CI + docs hygiene

- **CH-01.1** Add `.github/workflows/ci.yml` running `pnpm install --frozen-lockfile` →
  `pnpm lint` → `pnpm test` → `pnpm build`, on `pull_request` and `push` to
  `main`, so every PR is gated on lint/test/build.
- **CH-01.2** Fix the stale multi-theme/Phase-3 reference in `src/tokens/README.md`
  (regenerated by `toki` on each token change).
- **CH-01.3** Verify: `pnpm lint`, `pnpm test`, `pnpm build` pass locally; PR checks appear.

### CH-04 — Feedback components

- **CH-04.1** `ch-alert` (reuses `status.positive`/`warning` tokens), `ch-spinner`,
  `ch-tooltip` (lightweight CSS-positioned, accessible via `aria-describedby`).
- **CH-04.2** `ch-modal` (focus trap, ESC, scroll lock) is the heaviest — flag as optional
  within this increment if scope balloons.
- **CH-04.3** Verify: tests for each; a11y assertions (keyboard + aria) in specs.

## Deferred (needs 2.0, breaking)

- **CH-08** Form-associated custom elements (`ElementInternals`) so `ch-input`/`ch-select`
  participate in native form `FormData`/submission.
- **CH-09** Event payload / API changes.
- **CH-10** Multi-theme support — blocked on `toki` first-class multi-theme output.
